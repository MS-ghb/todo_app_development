<!DOCTYPE html>
<html lang='Ja'>
  <head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/style.css">
    <title>ToDo編集</title>
  </head>
  <body>
    <h1>ToDo編集ページ</h1>
    <table border="1" style="border-collapse: collapse">
      <tr>
        <th>id</th>
        <th>title</th>
        <th>description</th>
        <th>completed</th>
        <th>created_at</th>
        <th>updated_at</th>
      </tr>
      <tr>
        <td class="<%= "strikeout" if @record.completed %>"><%= @record.id %></td>
        <td class="<%= "strikeout" if @record.completed %>"><%= @record.title %></td>
        <td class="<%= "strikeout" if @record.completed %>"><%= @record.description %></td>
        <td class="<%= "strikeout" if @record.completed %>">
          <% if @record.completed %>
            完了
          <% else %>
            未完了
          <% end %>
        </td>
        <td class="<%= "strikeout" if @record.completed %>"><%= @record.created_at %></td>
        <td class="<%= "strikeout" if @record.completed %>"><%= @record.updated_at %></td>
      </tr>
    </table>
    <div class="post-forms">
      <h2>修正用フォーム</h2>
      <p>修正したいレコードのタイトル、タスクの説明を入力します。</p>
      <form action="/todos/<%= @record.id %>/update" method="post">
        <input type="text" name="title" placeholder="タイトル">
        <input type="text" name="description" placeholder="タスクの詳細">
        <input type="submit" value="投稿">
      </form>
    </div>
    <a href="/" class="to-index-btn">Topページに戻る</a>
  </body>
</html>
